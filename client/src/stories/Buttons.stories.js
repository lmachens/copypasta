import React from 'react';
import SubmitButton from '../components/SubmitButton';
import VoteButton from '../components/VoteButton';

export default {
  title: 'Buttons'
};

export const Submit = () => <SubmitButton />;
export const Vote = () => <VoteButton />;
